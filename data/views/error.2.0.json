{
    "message": "<%=message%>",
    "status": "<%=status%>"
    <% if (hasOwnProperty("stack")) { %>
    ,"stack": [
    <% stack.forEach(function ( item, i, arr){ %>
    "<%=item %>"
    <%= ( arr.length > 0 && i < arr.length-1 ) ? ',': '' %>
    <% }); %>
    ]
    <% } %>
}
